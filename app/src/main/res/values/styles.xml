<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="TxtUsuario"
            parent="@android:style/Widget.TextView">
            <item name="android:textColor">@color/txt_usuario</item>
            <item name="android:textSize">@dimen/size_usuario</item>
        </style>

        <style name="Txtdatos"
            parent="@android:style/Widget.TextView">
            <item name="android:textColor">@color/txt_datos</item>
            <item name="android:textSize">@dimen/size_datos</item>
        </style>

</resources>
